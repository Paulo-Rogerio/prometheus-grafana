groups:
- name: python
  rules:

  - alert: http_request
    expr: rate(http_request_total[1m]) > 50
    for: 10s
    labels:
      severity: hight
      team: python
    annotations:
      summary: "{{ $labels.instance }} many request"
      description: "{{ $labels.instance }} many request application Books."